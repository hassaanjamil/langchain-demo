[project]
name = "langchain-project"
version = "0.1.0"
description = "Lang chain sample demo project to work on different features of it"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "langchain>=1.3.6",
    "langchain-google-genai>=4.2.5",
    "python-dotenv>=1.2.2",
    "streamlit>=1.57.0"
]
